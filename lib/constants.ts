/** Shared upload/validation limits, kept in one place so client and server agree. */
export const MAX_FILE_BYTES = 10 * 1024 * 1024; // ~10 MB
export const MAX_PAGES = 50;
export const ACCEPTED_MIME = "application/pdf";
